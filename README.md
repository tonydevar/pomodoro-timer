# pomodoro-timer
Antigravity project: Build a Pomodoro Timer web application with a React frontend and an Express.js backend API. Frontend (React + Vite): A clean, modern timer UI with start/pause/reset controls. Configurable work duration (default 25 min) and break duration (default 5 min). Visual progress ring/circle that fills as the timer counts down. Session h
